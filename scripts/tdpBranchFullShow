#!/bin/bash

#---- Show the branch for all projects ----
for i in ./* ; do
  if [ -d "$i" ]; then
    directory=$(basename $i)
    if [[ $directory != build* ]]; then
      echo "=================================================================="
      echo $directory
      cd $directory
      tdpBranchShow
      cd ..
    fi
  fi
done
echo "=================================================================="
