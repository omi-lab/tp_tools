#!/bin/bash

#---- Status of all projects ----
for i in ./* ; do
  if [ -d "$i" ]; then
    directory=$(basename $i)
    if [[ $directory != build* ]]; then
      echo "=================================================================="
      echo $directory
      cd $directory
      tdpStatus
      cd ..
    fi
  fi
done
echo "=================================================================="
